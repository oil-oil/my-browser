#!/usr/bin/env node

console.error(`real-browser.mjs has been retired.

Use the My Browser CLI instead:
  scripts/my-browser --help

This old entry exits intentionally so new Agent workflows do not keep spreading the legacy command shape.`);
process.exit(2);
